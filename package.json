{
  "name": "gen-ai",
  "version": "0.1.0",
  "scripts": {
    "fix": "npm run format && npm run lint",
    "format": "npm run format:frontend && npm run format:infrastructure",
    "format:frontend": "cd frontend && npm run format",
    "format:infrastructure": "cd infrastructure && npm run format",
    "lint": "npm run lint:frontend && npm run lint:infrastructure",
    "lint:frontend": "cd frontend && npm run lint",
    "lint:infrastructure": "cd infrastructure && npm run lint",
    "setup": "cd infrastructure && npm install && cd ../frontend && npm install"
  }
}
